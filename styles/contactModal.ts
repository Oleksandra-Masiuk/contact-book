import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  buttonClose: {
    marginLeft: 'auto',
  },
  modalWrapper: {
    paddingVertical: 30,
    paddingHorizontal: 10,
    gap: 30,
  },
});

export {styles};
