import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  buttonClose: {
    marginLeft: 'auto',
    width: 40,
  },
  modalWrapper: {
    paddingVertical: 30,
    paddingHorizontal: 10,
    gap: 30,
  },
});

export {styles};
